@model Invoice_automation_V1.ViewModels.InvoiceListViewModel
@{
    ViewData["Title"] = "Invoices";
}

<div class="page-header flex justify-between items-center">
    <div>
        <h2 class="page-title">
            <i class="bi bi-file-text"></i> Invoices
        </h2>
        <p class="page-subtitle mb-0">Manage and track all your invoices</p>
    </div>
    <a asp-action="Create" class="btn btn-primary">
        <i class="bi bi-plus-circle"></i> New Invoice
    </a>
</div>

<!-- Filters -->
<div class="card mb-4">
    <div class="card-body">
        <form method="get" asp-action="Index" class="row g-3 items-end">
            <div class="col-md-3">
                <label class="form-label">Status</label>
                <select name="status" class="form-select" onchange="this.form.submit()">
                    <option value="">All Statuses</option>
                    <option value="Draft" selected="@(Model.StatusFilter == "Draft")">Draft</option>
                    <option value="PendingApproval" selected="@(Model.StatusFilter == "PendingApproval")">Pending Approval</option>
                    <option value="Approved" selected="@(Model.StatusFilter == "Approved")">Approved</option>
                    <option value="Rejected" selected="@(Model.StatusFilter == "Rejected")">Rejected</option>
                    <option value="Paid" selected="@(Model.StatusFilter == "Paid")">Paid</option>
                    <option value="Overdue" selected="@(Model.StatusFilter == "Overdue")">Overdue</option>
                </select>
            </div>
            <div class="col-md-7">
                <label class="form-label">Search</label>
                <input type="text" name="search" class="form-control" placeholder="Search by invoice number or vendor..." value="@Model.SearchQuery">
            </div>
            <div class="col-md-2">
                <button type="submit" class="btn btn-primary w-full">
                    <i class="bi bi-search"></i> Search
                </button>
            </div>
        </form>
    </div>
</div>

@if (Model.Invoices.Any())
{
    <div class="card">
        <div class="table-responsive">
            <table class="table table-hover mb-0">
                <thead>
                    <tr>
                        <th>Invoice #</th>
                        <th>Vendor</th>
                        <th>Date</th>
                        <th>Due Date</th>
                        <th>Amount</th>
                        <th>Status</th>
                        <th>OCR</th>
                        <th>Actions</th>
                    </tr>
                </thead>
                <tbody>
                    @foreach (var invoice in Model.Invoices)
                    {
                        <tr>
                            <td>
                                <a asp-action="Details" asp-route-id="@invoice.Id" class="font-bold" style="color: var(--primary-color);">
                                    @invoice.InvoiceNumber
                                </a>
                            </td>
                            <td>@invoice.VendorName</td>
                            <td>@invoice.InvoiceDate.ToString("MMM dd, yyyy")</td>
                            <td>
                                @if (invoice.DueDate.HasValue)
                                {
                                    <span class="@(invoice.IsOverdue ? "text-danger font-medium" : "")">
                                        @invoice.DueDate.Value.ToString("MMM dd, yyyy")
                                        @if (invoice.IsOverdue)
                                        {
                                            <i class="bi bi-exclamation-triangle-fill ms-1"></i>
                                        }
                                    </span>
                                }
                                else
                                {
                                    <span style="color: var(--text-muted);">&mdash;</span>
                                }
                            </td>
                            <td class="font-bold">@invoice.Currency @invoice.TotalAmount.ToString("N2")</td>
                            <td>
                                @{
                                    var badgeClass = invoice.Status switch
                                    {
                                        "Draft" => "bg-secondary",
                                        "PendingApproval" => "bg-warning",
                                        "Approved" => "bg-success",
                                        "Rejected" => "bg-danger",
                                        "Paid" => "bg-primary",
                                        "Overdue" => "bg-danger",
                                        _ => "bg-secondary"
                                    };
                                }
                                <span class="badge @badgeClass">@invoice.Status</span>
                            </td>
                            <td>
                                @if (invoice.IsOcrProcessed)
                                {
                                    <span class="badge" style="background-color: var(--success-light); color: #065f46;">
                                        <i class="bi bi-check-circle"></i> Processed
                                    </span>
                                }
                                else
                                {
                                    <span class="badge" style="background-color: var(--gray-100); color: var(--text-tertiary);">
                                        <i class="bi bi-hourglass-split"></i> Pending
                                    </span>
                                }
                            </td>
                            <td>
                                <div class="flex gap-1">
                                    <a asp-action="Details" asp-route-id="@invoice.Id" class="btn btn-sm btn-outline-primary" title="View Details">
                                        <i class="bi bi-eye"></i>
                                    </a>
                                    <a asp-action="Edit" asp-route-id="@invoice.Id" class="btn btn-sm btn-outline-secondary" title="Edit">
                                        <i class="bi bi-pencil"></i>
                                    </a>
                                </div>
                            </td>
                        </tr>
                    }
                </tbody>
            </table>
        </div>

        <!-- Pagination -->
        @if (Model.TotalCount > Model.PageSize)
        {
            var totalPages = (int)Math.Ceiling((double)Model.TotalCount / Model.PageSize);
            <div class="card-body" style="border-top: 1px solid var(--border-color);">
                <nav>
                    <ul class="pagination justify-center mb-0">
                        <li class="page-item @(Model.PageNumber <= 1 ? "disabled" : "")">
                            <a class="page-link" asp-action="Index" asp-route-page="@(Model.PageNumber - 1)" asp-route-status="@Model.StatusFilter" asp-route-search="@Model.SearchQuery">Previous</a>
                        </li>
                        @for (int i = 1; i <= totalPages; i++)
                        {
                            <li class="page-item @(i == Model.PageNumber ? "active" : "")">
                                <a class="page-link" asp-action="Index" asp-route-page="@i" asp-route-status="@Model.StatusFilter" asp-route-search="@Model.SearchQuery">@i</a>
                            </li>
                        }
                        <li class="page-item @(Model.PageNumber >= totalPages ? "disabled" : "")">
                            <a class="page-link" asp-action="Index" asp-route-page="@(Model.PageNumber + 1)" asp-route-status="@Model.StatusFilter" asp-route-search="@Model.SearchQuery">Next</a>
                        </li>
                    </ul>
                </nav>
            </div>
        }

        <div class="card-body pt-0" style="@(Model.TotalCount <= Model.PageSize ? "border-top: 1px solid var(--border-color);" : "")">
            <p class="mb-0 text-center" style="color: var(--text-tertiary); font-size: 0.8125rem;">
                Showing @Model.Invoices.Count of @Model.TotalCount total invoices
            </p>
        </div>
    </div>
}
else
{
    <div class="card">
        <div class="empty-state">
            <i class="bi bi-file-text"></i>
            <h5>No Invoices Found</h5>
            <p>
                @if (!string.IsNullOrWhiteSpace(Model.SearchQuery) || !string.IsNullOrWhiteSpace(Model.StatusFilter))
                {
                    <span>No invoices match your search criteria. Try adjusting your filters.</span>
                }
                else
                {
                    <span>Get started by uploading your first invoice.</span>
                }
            </p>
            <a asp-action="Create" class="btn btn-primary">
                <i class="bi bi-plus-circle"></i> Upload New Invoice
            </a>
        </div>
    </div>
}
