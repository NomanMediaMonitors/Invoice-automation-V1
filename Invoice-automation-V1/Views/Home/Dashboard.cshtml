@model Invoice_automation_V1.ViewModels.DashboardViewModel

@{
    ViewData["Title"] = "Dashboard";
}

<!-- Page Header -->
<div class="page-header">
    <div class="d-flex justify-content-between align-items-center flex-wrap gap-3">
        <div>
            <div class="page-title">
                <h2 class="fw-bold mb-0">Welcome back, @Model.UserFullName</h2>
            </div>
            <p class="page-subtitle mb-0">
                Here's what's happening across your organization today, @DateTime.Now.ToString("MMMM dd, yyyy").
            </p>
        </div>
        @if (Model.HasAnyCompanies)
        {
            <a asp-controller="Company" asp-action="Index" class="btn btn-sm btn-outline-primary">
                <i class="bi bi-building me-1"></i> Manage Companies
            </a>
        }
    </div>
</div>

<!-- Stat Cards -->
<div class="row g-3 mb-4">
    <div class="col-6 col-lg-3">
        <a asp-controller="Company" asp-action="Index" class="text-decoration-none d-block">
            <div class="stat-card">
                <div class="stat-card-icon" style="background-color: var(--primary-50); color: var(--primary-color);">
                    <i class="bi bi-building"></i>
                </div>
                <div class="stat-card-value">@Model.Stats.TotalCompanies</div>
                <div class="stat-card-label">Companies</div>
            </div>
        </a>
    </div>
    <div class="col-6 col-lg-3">
        <a asp-controller="Vendor" asp-action="SelectCompany" class="text-decoration-none d-block">
            <div class="stat-card">
                <div class="stat-card-icon" style="background-color: var(--success-light); color: var(--success-color);">
                    <i class="bi bi-truck"></i>
                </div>
                <div class="stat-card-value">@Model.Stats.TotalVendors</div>
                <div class="stat-card-label">Vendors</div>
            </div>
        </a>
    </div>
    <div class="col-6 col-lg-3">
        <a asp-controller="Invoice" asp-action="SelectCompany" class="text-decoration-none d-block">
            <div class="stat-card">
                <div class="stat-card-icon" style="background-color: var(--info-light); color: var(--info-color);">
                    <i class="bi bi-receipt"></i>
                </div>
                <div class="stat-card-value">@Model.Stats.TotalInvoices</div>
                <div class="stat-card-label">Total Invoices</div>
            </div>
        </a>
    </div>
    <div class="col-6 col-lg-3">
        <a asp-controller="Invoice" asp-action="SelectCompany" class="text-decoration-none d-block">
            <div class="stat-card">
                <div class="stat-card-icon" style="background-color: #ecfdf5; color: #059669;">
                    <i class="bi bi-check-circle"></i>
                </div>
                <div class="stat-card-value">@Model.Stats.ApprovedInvoices</div>
                <div class="stat-card-label">Approved</div>
            </div>
        </a>
    </div>
</div>

<!-- Main Content: Quick Actions + Recent Activity -->
<div class="row g-3">
    <!-- Quick Actions -->
    <div class="col-lg-4">
        <div class="stat-card" style="padding: 0;">
            <div style="padding: 1rem 1.25rem; border-bottom: 1px solid var(--border-color);">
                <h6 class="fw-semibold mb-0" style="color: var(--text-primary); font-size: 0.8125rem; text-transform: uppercase; letter-spacing: 0.04em;">Quick Actions</h6>
            </div>
            <div style="padding: 1rem 1.25rem;">
                <div class="d-grid gap-2">
                    <a asp-controller="Company" asp-action="Create" class="btn btn-sm btn-outline-primary text-start d-flex align-items-center gap-2">
                        <i class="bi bi-plus-circle"></i>
                        <span>
                            <span class="fw-semibold d-block" style="font-size: 0.8125rem;">Add New Company</span>
                            <span style="font-size: 0.6875rem; color: var(--text-tertiary);">Register a new business entity</span>
                        </span>
                    </a>
                    @if (Model.HasAnyCompanies)
                    {
                        <a asp-controller="Vendor" asp-action="SelectCompany" class="btn btn-sm btn-outline-primary text-start d-flex align-items-center gap-2">
                            <i class="bi bi-truck"></i>
                            <span>
                                <span class="fw-semibold d-block" style="font-size: 0.8125rem;">Add Vendor</span>
                                <span style="font-size: 0.6875rem; color: var(--text-tertiary);">Select a company, then register a supplier</span>
                            </span>
                        </a>
                        <a asp-controller="Employee" asp-action="SelectCompany" asp-route-target="Invite" class="btn btn-sm btn-outline-primary text-start d-flex align-items-center gap-2">
                            <i class="bi bi-person-plus"></i>
                            <span>
                                <span class="fw-semibold d-block" style="font-size: 0.8125rem;">Invite Team Member</span>
                                <span style="font-size: 0.6875rem; color: var(--text-tertiary);">Select a company, then invite users</span>
                            </span>
                        </a>
                    }
                    else
                    {
                        <button class="btn btn-sm btn-outline-secondary text-start d-flex align-items-center gap-2" disabled>
                            <i class="bi bi-upload"></i>
                            <span>
                                <span class="fw-semibold d-block" style="font-size: 0.8125rem;">Upload Invoice</span>
                                <span style="font-size: 0.6875rem; color: var(--text-tertiary);">Create a company first</span>
                            </span>
                        </button>
                        <button class="btn btn-sm btn-outline-secondary text-start d-flex align-items-center gap-2" disabled>
                            <i class="bi bi-truck"></i>
                            <span>
                                <span class="fw-semibold d-block" style="font-size: 0.8125rem;">Add Vendor</span>
                                <span style="font-size: 0.6875rem; color: var(--text-tertiary);">Create a company first</span>
                            </span>
                        </button>
                    }
                </div>
            </div>
        </div>
    </div>

    <!-- Recent Activity -->
    <div class="col-lg-8">
        <div class="stat-card" style="padding: 0;">
            <div style="padding: 1rem 1.25rem; border-bottom: 1px solid var(--border-color); display: flex; justify-content: space-between; align-items: center;">
                <h6 class="fw-semibold mb-0" style="color: var(--text-primary); font-size: 0.8125rem; text-transform: uppercase; letter-spacing: 0.04em;">Recent Activity</h6>
                @if (Model.RecentActivities.Any())
                {
                    <span class="badge bg-primary">@Model.RecentActivities.Count</span>
                }
            </div>
            <div style="padding: 0;">
                @if (Model.RecentActivities.Any())
                {
                    <table class="table table-hover mb-0">
                        <thead>
                            <tr>
                                <th style="width: 40px;"></th>
                                <th>Event</th>
                                <th>Details</th>
                                <th style="text-align: right;">Time</th>
                            </tr>
                        </thead>
                        <tbody>
                            @foreach (var activity in Model.RecentActivities)
                            {
                                <tr>
                                    <td>
                                        <span class="d-inline-flex align-items-center justify-content-center" style="width: 28px; height: 28px; border-radius: var(--radius-md); background-color: @(activity.IconColor == "success" ? "var(--success-light)" : "var(--primary-50)"); color: @(activity.IconColor == "success" ? "var(--success-color)" : "var(--primary-color)"); font-size: 0.8125rem;">
                                            <i class="bi bi-@activity.Icon"></i>
                                        </span>
                                    </td>
                                    <td class="fw-medium" style="color: var(--text-primary);">@activity.Title</td>
                                    <td style="color: var(--text-secondary);">@activity.Description</td>
                                    <td style="text-align: right; white-space: nowrap; color: var(--text-tertiary); font-size: 0.75rem;">@activity.TimeAgo</td>
                                </tr>
                            }
                        </tbody>
                    </table>
                }
                else
                {
                    <div class="empty-state">
                        <i class="bi bi-inbox"></i>
                        <h5>No recent activity</h5>
                        <p>Your recent activities will appear here as you start using the platform.</p>
                        @if (!Model.HasAnyCompanies)
                        {
                            <a asp-controller="Company" asp-action="Create" class="btn btn-sm btn-primary">
                                <i class="bi bi-plus-circle me-1"></i> Create Your First Company
                            </a>
                        }
                    </div>
                }
            </div>
        </div>
    </div>
</div>
